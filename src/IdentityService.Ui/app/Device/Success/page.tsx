import { DeviceSuccessPage } from "@/components/authorization-pages";

export const metadata = { title: "Device request complete" };

export default function Page() {
  return <DeviceSuccessPage />;
}
